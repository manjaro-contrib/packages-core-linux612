From dd53c29e6062673609906a8ebd23ce8038b46f90 Mon Sep 17 00:00:00 2001
From: Luke Jones <luke@ljones.dev>
Date: Fri, 18 Apr 2025 11:19:02 +1200
Subject: [PATCH 9/9] test touchpad maps

---
 .../zotac-zone-hid/zotac-zone-hid-config.c    | 42 ++++++++++++-------
 .../hid/zotac-zone-hid/zotac-zone-hid-core.c  |  2 -
 2 files changed, 28 insertions(+), 16 deletions(-)

diff --git a/drivers/hid/zotac-zone-hid/zotac-zone-hid-config.c b/drivers/hid/zotac-zone-hid/zotac-zone-hid-config.c
index e47858a3fab9..150d554f10d9 100644
--- a/drivers/hid/zotac-zone-hid/zotac-zone-hid-config.c
+++ b/drivers/hid/zotac-zone-hid/zotac-zone-hid-config.c
@@ -193,7 +193,7 @@ static const struct button_mapping_entry button_map[] = {
 static const struct {
 	const char *name;
 	u8 id;
-} button_names[] = {
+} gamepad_button_names[] = {
 	{ "none", BUTTON_NONE },
 	{ "a", BUTTON_A },
 	{ "b", BUTTON_B },
@@ -209,16 +209,6 @@ static const struct {
 	{ "dpad_down", BUTTON_DPAD_DOWN },
 	{ "dpad_left", BUTTON_DPAD_LEFT },
 	{ "dpad_right", BUTTON_DPAD_RIGHT },
-	{ "m1", BUTTON_M1 },
-	{ "m2", BUTTON_M2 },
-	{ "l_touch_up", BUTTON_L_TOUCH_UP },
-	{ "l_touch_down", BUTTON_L_TOUCH_DOWN },
-	{ "l_touch_left", BUTTON_L_TOUCH_LEFT },
-	{ "l_touch_right", BUTTON_L_TOUCH_RIGHT },
-	{ "r_touch_up", BUTTON_R_TOUCH_UP },
-	{ "r_touch_down", BUTTON_R_TOUCH_DOWN },
-	{ "r_touch_left", BUTTON_R_TOUCH_LEFT },
-	{ "r_touch_right", BUTTON_R_TOUCH_RIGHT },
 };
 
 static const struct {
@@ -1101,9 +1091,9 @@ static ssize_t gamepad_list_show(struct device *dev,
 	char *p = buf;
 	int i;
 
-	for (i = 0; i < ARRAY_SIZE(button_names); i++) {
-		if (button_names[i].id != BUTTON_NONE)
-			p += sprintf(p, "%s ", button_names[i].name);
+	for (i = 0; i < ARRAY_SIZE(gamepad_button_names); i++) {
+		if (gamepad_button_names[i].id != BUTTON_NONE)
+			p += sprintf(p, "%s ", gamepad_button_names[i].name);
 	}
 
 	if (p > buf)
@@ -1212,6 +1202,14 @@ DEFINE_BUTTON_REMAP_ATTRS(dpad_left, BUTTON_DPAD_LEFT);
 DEFINE_BUTTON_REMAP_ATTRS(dpad_right, BUTTON_DPAD_RIGHT);
 DEFINE_BUTTON_REMAP_ATTRS(btn_m1, BUTTON_M1);
 DEFINE_BUTTON_REMAP_ATTRS(btn_m2, BUTTON_M2);
+DEFINE_BUTTON_REMAP_ATTRS(left_touchpad_up, BUTTON_L_TOUCH_UP);
+DEFINE_BUTTON_REMAP_ATTRS(left_touchpad_down, BUTTON_L_TOUCH_DOWN);
+DEFINE_BUTTON_REMAP_ATTRS(left_touchpad_left, BUTTON_L_TOUCH_LEFT);
+DEFINE_BUTTON_REMAP_ATTRS(left_touchpad_right, BUTTON_L_TOUCH_RIGHT);
+DEFINE_BUTTON_REMAP_ATTRS(right_touchpad_up, BUTTON_R_TOUCH_UP);
+DEFINE_BUTTON_REMAP_ATTRS(right_touchpad_down, BUTTON_R_TOUCH_DOWN);
+DEFINE_BUTTON_REMAP_ATTRS(right_touchpad_left, BUTTON_R_TOUCH_LEFT);
+DEFINE_BUTTON_REMAP_ATTRS(right_touchpad_right, BUTTON_R_TOUCH_RIGHT);
 
 static int zotac_get_button_turbo(struct zotac_device *zotac)
 {
@@ -1400,6 +1398,14 @@ DEFINE_BUTTON_GROUP_NO_TURBO(dpad_left, BUTTON_DPAD_LEFT);
 DEFINE_BUTTON_GROUP_NO_TURBO(dpad_right, BUTTON_DPAD_RIGHT);
 DEFINE_BUTTON_GROUP_NO_TURBO(btn_m1, BUTTON_M1);
 DEFINE_BUTTON_GROUP_NO_TURBO(btn_m2, BUTTON_M2);
+DEFINE_BUTTON_GROUP_NO_TURBO(left_touchpad_up, BUTTON_L_TOUCH_UP);
+DEFINE_BUTTON_GROUP_NO_TURBO(left_touchpad_down, BUTTON_L_TOUCH_DOWN);
+DEFINE_BUTTON_GROUP_NO_TURBO(left_touchpad_left, BUTTON_L_TOUCH_LEFT);
+DEFINE_BUTTON_GROUP_NO_TURBO(left_touchpad_right, BUTTON_L_TOUCH_RIGHT);
+DEFINE_BUTTON_GROUP_NO_TURBO(right_touchpad_up, BUTTON_R_TOUCH_UP);
+DEFINE_BUTTON_GROUP_NO_TURBO(right_touchpad_down, BUTTON_R_TOUCH_DOWN);
+DEFINE_BUTTON_GROUP_NO_TURBO(right_touchpad_left, BUTTON_R_TOUCH_LEFT);
+DEFINE_BUTTON_GROUP_NO_TURBO(right_touchpad_right, BUTTON_R_TOUCH_RIGHT);
 
 /* Define all button directories */
 static struct button_directory button_dirs[] = {
@@ -1419,6 +1425,14 @@ static struct button_directory button_dirs[] = {
 	DEFINE_BUTTON_DIR_NO_TURBO(dpad_right, BUTTON_DPAD_RIGHT),
 	DEFINE_BUTTON_DIR_NO_TURBO(btn_m1, BUTTON_M1),
 	DEFINE_BUTTON_DIR_NO_TURBO(btn_m2, BUTTON_M2),
+	DEFINE_BUTTON_DIR_NO_TURBO(left_touchpad_up, BUTTON_L_TOUCH_UP),
+	DEFINE_BUTTON_DIR_NO_TURBO(left_touchpad_down, BUTTON_L_TOUCH_DOWN),
+	DEFINE_BUTTON_DIR_NO_TURBO(left_touchpad_left, BUTTON_L_TOUCH_LEFT),
+	DEFINE_BUTTON_DIR_NO_TURBO(left_touchpad_right, BUTTON_L_TOUCH_RIGHT),
+	DEFINE_BUTTON_DIR_NO_TURBO(right_touchpad_up, BUTTON_R_TOUCH_UP),
+	DEFINE_BUTTON_DIR_NO_TURBO(right_touchpad_down, BUTTON_R_TOUCH_DOWN),
+	DEFINE_BUTTON_DIR_NO_TURBO(right_touchpad_left, BUTTON_R_TOUCH_LEFT),
+	DEFINE_BUTTON_DIR_NO_TURBO(right_touchpad_right, BUTTON_R_TOUCH_RIGHT),
 	{ NULL, 0, false, NULL, NULL, NULL } /* Terminator */
 };
 
diff --git a/drivers/hid/zotac-zone-hid/zotac-zone-hid-core.c b/drivers/hid/zotac-zone-hid/zotac-zone-hid-core.c
index ef31c9d7aa07..be5a266c6a12 100644
--- a/drivers/hid/zotac-zone-hid/zotac-zone-hid-core.c
+++ b/drivers/hid/zotac-zone-hid/zotac-zone-hid-core.c
@@ -90,13 +90,11 @@ static void process_dial_wheel_report(struct zotac_device *zotac, u8 *data,
 
 	if (value & BIT(ZOTAC_RIGHT_DIAL_CW_BIT))
 		input_report_rel(zotac->wheel_input, REL_WHEEL, 1);
-
 	if (value & BIT(ZOTAC_RIGHT_DIAL_CCW_BIT))
 		input_report_rel(zotac->wheel_input, REL_WHEEL, -1);
 
 	if (value & BIT(ZOTAC_LEFT_DIAL_CW_BIT))
 		input_report_rel(zotac->wheel_input, REL_HWHEEL, 1);
-
 	if (value & BIT(ZOTAC_LEFT_DIAL_CCW_BIT))
 		input_report_rel(zotac->wheel_input, REL_HWHEEL, -1);
 
-- 
2.49.0

